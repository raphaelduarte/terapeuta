<%= form_with(model: historia_medica_pregressa, class: "contents") do |form| %>
  <% if historia_medica_pregressa.errors.any? %>
    <div id="error_explanation" class="bg-red-50 text-red-500 px-3 py-2 font-medium rounded-lg mt-3">
      <h2><%= pluralize(historia_medica_pregressa.errors.count, "error") %> prohibited this historia_medica_pregressa from being saved:</h2>

      <ul>
        <% historia_medica_pregressa.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="inline">
    <%= form.submit class: "rounded-lg py-3 px-5 bg-blue-600 text-white inline-block font-medium cursor-pointer" %>
  </div>
<% end %>
